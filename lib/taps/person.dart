import 'package:flutter/material.dart';

class Persson extends StatelessWidget {
  const Persson({super.key});
static const routName="Persson";
  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.red,);
  }
}