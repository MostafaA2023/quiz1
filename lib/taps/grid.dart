import 'package:flutter/material.dart';

class Grid extends StatelessWidget {
  const Grid({super.key});
static const routName="Grid";
  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.blue,);
  }
}